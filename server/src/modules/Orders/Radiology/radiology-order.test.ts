/**
 * Placeholder. This order path's HTTP integration test is written when issue #4's A1.2b wiring
 * reaches it (see Orders/Laboratory/lab-order.test.ts for the pattern: shared fixtures, catalogue
 * seeding, and an outbox-emission assertion).
 */
describe.skip('radiology order endpoints (pending A1.2b)', () => {
  it('is covered when the path is wired', () => {
    expect(true).toBe(true);
  });
});
