{
  "name": "@ehmrs/phi-scrubber",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic PHI scrubber for the fixture pipeline (ADR-0005)",
  "type": "module",
  "bin": {
    "phi-scrub": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
