{
  "name": "@ehmrs/token-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "ADR-0001/0009 token pipeline — DESIGN.md feeds both the PrimeVue preset and the Tailwind config (issue #40)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "generate": "tsc -p tsconfig.json && node dist/cli.js",
    "check": "tsc -p tsconfig.json && node dist/cli.js --check"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
